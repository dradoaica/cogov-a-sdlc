# Gemini CLI instructions (adapter)

Follow the canonical repository agent standard:

- `.agents/AGENTS.md`

If these instructions conflict with the repository standard, the repository standard wins.

You may extend this adapter with Gemini-CLI-specific mechanics only (for example, tool or model settings), not with
contradictory governance rules.
